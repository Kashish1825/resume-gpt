# =============================================================
#  requirements.txt  —  Python Dependencies (FREE VERSION)
# =============================================================
#  Install karo:
#    pip install -r requirements.txt
#
#  OpenAI HATAYA gaya hai — ab GROQ use hota hai jo BILKUL FREE hai!
# =============================================================

flask==3.0.0          # Web framework — HTTP routes handle karta hai
flask-cors==4.0.0     # Frontend JS ko backend se baat karne deta hai
werkzeug==3.0.1       # Secure file upload ke liye

groq==0.9.0           # Groq API client — FREE, GPT jaisi quality, ultra fast!

pymupdf==1.23.8       # PDF file se text nikaalne ke liye
python-docx==1.1.0    # Word DOCX file se text nikaalne ke liye

python-dotenv==1.0.0  # .env file se GROQ_API_KEY load karne ke liye

# SQLite Python ke saath already aata hai — koi install nahi!
